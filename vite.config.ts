import { defineConfig } from "vite";

export default defineConfig({
  // Use relative URLs so the build works on GitHub Pages
  // regardless of whether it's hosted at / or /<repo>/.
  base: "./",
});

